export default function Loading() {
  return (
    <main id="main" className="page" aria-busy="true" aria-live="polite">
      <div className="shell">
        <p className="eyebrow">Loading the atlas…</p>
      </div>
    </main>
  );
}
